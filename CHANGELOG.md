# HelloWorld 변경 내역

## 번역 결과 크게 보기

- 번역 결과 문장을 누르면 상대방에게 보여주기 좋은 전체화면으로 확대
- 확대 화면에 발음 표기 또는 원문을 함께 표시
- 닫기 버튼, 배경 클릭, Esc 키로 확대 화면 닫기 지원
- 서비스 워커 캐시를 `helloworld-static-v12`로 갱신

---

## 장소 질문·번역 기록 개선

- 장소를 선택하면 `어디예요?`와 `어떻게 가나요?` 표현을 동시에 표시
- Translate의 자주 쓰는 문장 영역 제거
- 최근 번역 저장 한도를 12개에서 30개로 확대
- 서비스 워커 캐시를 `helloworld-static-v11`로 갱신

---

## 표현·선택 기능 보강

- Translate 하단에 오프라인 사전·온라인 번역·발음 표기 안내 추가
- 길 묻기에 장소와 질문 유형을 선택하는 표현 생성기 추가
- 카페 메뉴를 확대하고 음료 종류별로 유효한 주문 옵션만 표시
- 다른 아코디언이나 탭으로 이동하면 주문 선택 상태와 생성 문장 초기화
- 숫자에 20, 100, 1,000, 10,000, 100,000, 1,000,000 단위 추가
- 서비스 워커 캐시를 `helloworld-static-v10`으로 갱신

---

## 로컬 파일 직접 실행 복구

- `file://` 환경에서 ES 모듈과 JSON `fetch()`가 차단되어 로딩 화면에 멈추던 문제 수정
- 모듈 소스와 JSON에서 자동 생성되는 로컬 실행용 클래식 번들 추가
- GitHub Pages에서는 기존 ES 모듈 구조를 그대로 사용
- GitHub Actions에서 생성 파일이 소스와 일치하는지 검증
- 서비스 워커 캐시를 `helloworld-static-v8`로 갱신

---

## UX 개선

- 언어 화면 상단에 국가 선택 홈으로 돌아가는 버튼 추가
- 같은 단계의 아코디언은 하나만 열리고 탭 이동 시 열린 패널이 닫히도록 변경
- 음식점·카페에서 메뉴와 옵션 선택 후 현지어 표현을 생성하는 주문 도우미 추가
- 기초 단어의 하위 아코디언 카드를 2열로 표시
- Let's Talk 주제 선택 후 전용 대화 화면으로 전환하고 주제 선택으로 복귀 가능
- Translate 탭에 자주 쓰는 문장 바로 번역 기능 추가
- 서비스 워커 캐시를 `helloworld-static-v7`로 갱신

---

## 공통 정적 앱 구조 개편

- 국가별로 중복되던 앱을 `app.html`과 `js/app.js` 기반의 공통 앱으로 통합
- 기존 `/indonesia/`, `/vietnam/` 주소는 공통 앱으로 연결되는 호환 진입점으로 유지
- 언어별 JSON을 유일한 콘텐츠 원본으로 사용하고 중복 `.js` 데이터 제거
- 데이터 로딩, 번역, 기록 저장을 독립적인 서비스 모듈로 분리
- 인라인 이벤트 대신 공통 이벤트 위임 적용
- `scripts/validate-data.mjs`와 GitHub Actions 데이터 검증 추가
- 서비스 워커가 동일 출처의 정적 자원만 캐시하고 문서 요청만 HTML로 폴백하도록 수정
- 캐시 버전을 `helloworld-static-v6`로 갱신

---

## 이전 베트남 페이지 수정 내역

수정 대상: `vietnam/index.html`, `css/style.css`, `index.html`, `sw.js`

---

## A. 치명적 오류

### A1. 발음 엔진 정규식 이중 이스케이프
`viPron()` 안의 정규식 리터럴 5곳이 `\\d`, `\\s`, `\\p{L}`로 되어 있었습니다.
정규식 리터럴에서 `\\d`는 "숫자"가 아니라 "백슬래시 + d"이므로 치환 토큰이
복원되지도 제거되지도 않았고, 화면에 `§33§`이 그대로 노출됐습니다.

- Let's Talk 250문장 중 **139개**가 이 상태였음
- 키오스크 결과와 온라인 번역 결과 발음도 동일
- 조치: 백슬래시를 한 단계씩 제거

### A2. 치환 토큰 인덱스 불일치
토큰 번호는 정렬된 `phrases` 배열의 인덱스인데 값은 `marked` 배열에 순차로
쌓여, A1만 고치면 엉뚱한 단어의 발음이 들어갔습니다.
- 조치: `marked.length`를 토큰 번호로 사용

### A3. 키오스크가 인도네시아 메뉴
`KIOSK_DATA`가 인도네시아어 원본이고 `VI_KIOSK_MAP`이 음식·장소 이름을
덮지 못했습니다.

- `Tôi muốn Nasi goreng, ya.` — 인도네시아 음식 + 인도네시아 어조사 `, ya.`
- `Ở đâu toilet?` — `viKioskText()`가 접두사 매칭 후 즉시 return 하여
  장소 이름이 번역되지 않았고, 어순도 틀렸음 (`Nhà vệ sinh ở đâu?`가 맞음)
- 이 때문에 `VI_KIOSK_MAP`의 장소명 12개 항목이 죽은 코드였음

조치:
- 메뉴를 베트남 음식으로 전면 교체 (phở, bún chả, bánh mì, cơm tấm,
  bánh xèo, gỏi cuốn, nem rán, cà phê sữa đá, trà sữa 등)
- `, ya.` 제거, 문장 템플릿을 `Cho tôi ~` / `Có ~ không?`로 변경
- 장소 문장을 `<장소> ở đâu?` 어순으로 수정
- `viKioskText()`와 `VI_KIOSK_MAP` 삭제

### A4. 오프라인 사전에 인도네시아어 35개
`VOCAB` 100개가 인도네시아어였고, DOM의 Basic Phrases에서 한국어로 매칭되는
65개만 구제됐습니다. 나머지 35개는 인도네시아어 그대로 노출됐습니다.

    빵 → Roti      돈 → Uang      학교 → Sekolah
    축구 → Sepak bola            출구 → Pintu keluar
    {ko:"인도네시아", id:"Vietnam"}  ← 항목 자체가 꼬임

조치: `VOCAB` 100개와 `EXTRA` 26개를 베트남어로 새로 작성. DOM 스크레이핑
방식 폐기.

---

## B. 내용 오류

- **B1** 헤더 `Halo Vietnam` → `Xin chào Việt Nam` (Halo는 인도네시아어)
- **B2** `할랄 / Halal` 항목 삭제 (기초 단어 + 키오스크 질문)
- **B3** 인도네시아 고유 내용 교체
  - 른당 → 반쎄오 / 나시고렝 → 볶음밥 / 사테 → 분짜 / 삼발 → 고추 소스
  - "인도네시아 팝/가수" → "베트남 팝/가수"
  - "인도네시아에 대해 이야기해줄래요?" → "베트남에 대해…"
  - 분짜 꼬리질문의 "닭/소고기", "땅콩 소스" → 문맥에 맞게 수정
- **B4** 조사 오류 수정. `josaEunNeun()` 도입
  - 화장실**는** → 화장실**은**, 공항**는** → 공항**은**, ATM**는** → ATM**은**
- **B5** 발음 이중 소스 통일 (화면 표기 vs 엔진 사전)

  | 단어 | 이전(화면) | 이전(엔진) | 수정 |
  |---|---|---|---|
  | Nhanh | 빠인 | 냔 | 냐인 |
  | Người | 응어이 | 응으어이 | 응으어이 |
  | Dễ | 데 | 제 | 제 |
  | Bận | 반 | 번 | 번 |
  | Mệt | 멋 | 멧 | 멧 |
  | Hiểu | 히우 | 히에우 | 히에우 |
  | Chờ | 쪄 | 쩌 | 쩌 |
  | Tính | 띵 | — | 띤 |

  이 밖에 Tìm 띵→띰, Nhỏ 녀→뇨, Lạnh 란→라인, Khách sạn 칵 산→칵 싼

---

## C. 구조 정리

### C1. 치환 레이어 제거 (가장 큰 변경)
인도네시아어 문장을 키로 쓰던 구조를 걷어냈습니다.

    이전:  topics(인도네시아어) → VI_TOPIC_MAP["Apa makanan..."] → 베트남어
    이후:  topics(베트남어 확정)

- 250문장을 `{ko, vi, pron}` 형태로 확정 저장
- `VI_TOPIC_MAP`, `VI_KIOSK_MAP`, `VI_EXTRA_MAP`,
  `applyStaticVietnameseTopics()` 전부 삭제
- 실행 중 발음 재생성(`q.pron=viPron(q.id)`) 제거

이제 원본 문장의 마침표가 바뀌어도 베트남어가 사라지지 않습니다.

### C2. 죽은 인도네시아 변환 코드 삭제
`CONS_ONSET`, `CONS_STAND`, `CODA`, `V_PLAIN`, `V_Y`, `V_W`, `DIGRAPHS`,
`E_WORDS`, `SPECIAL`, `isVowel()`, `syl()` 약 100줄 제거.
`transliterate()`는 온라인 번역 결과용 보조 함수로만 남김.

### C3. 중복 키 정리
`VI_PRON_WORDS` 10개(bạn, vui, thể, học, gần, cây, trái, bao, sẽ, việc),
`VI_PRON_PHRASES` 1개(hàn quốc), 인도네시아 지명 `pulau jeju` 제거.

### C4. 죽은 주제 제목 매핑 제거
키가 `"Ẩm thực & Cà phê"`인데 실제 값은 `"🍜 음식 & 카페 · Ẩm thực & Cà phê"`라
전체 일치가 되지 않던 5개 항목.

### C5. 서비스워커 캐시 버전
`helloworld-static-vietnam-2` → `helloworld-static-v3`

**중요:** cache-first 전략이므로, 앞으로 내용을 고칠 때마다 `sw.js`의
`CACHE` 문자열을 `-v4`, `-v5`로 올려야 기존 설치자에게 반영됩니다.

### C6. 필드/클래스 이름
베트남어 텍스트가 `class="id"`, 데이터 필드 `id:`에 담겨 있던 것을 `vi`로
변경(160곳). `css/style.css`는 `.id, .vi` 양쪽 선택자를 갖도록 확장해
인도네시아 페이지는 기존대로 동작합니다.

### C7. 발음 사전 확장
데이터에 등장하지만 사전에 없어 로마자로 남던 단어 **194개** 추가.
총 237개 → 492개.

### C8. 루트 페이지 CSS 통합
`index.html`의 인라인 `<style>`을 `css/style.css`로 이동.

---

## 검증 결과

    JS 문법                    OK
    UI 핸들러 실행             PASS 192 / FAIL 0
    Let's Talk § 깨짐          139 → 0 / 250
    발음 내 로마자 잔존        194 → 0
    오프라인 사전 인도네시아어  35 → 0
    동일 문장 발음 불일치       8 → 0
    사전 총 항목               461개

---

## 남은 일 — 현지 검수

베트남어 문장과 한글 발음은 **현지 검수가 필요합니다.**
특히 Let's Talk 250문장의 발음은 기존 매핑을 바탕으로 생성한 뒤 데이터로
확정한 것이라, 성조와 북부·남부 차이가 반영되어 있지 않습니다.

`발음검수_베트남어.csv` (626행)를 함께 넣었습니다. "수정할 발음" 칸만
채워서 돌려받으면 반영할 수 있습니다.

## 다음 단계 (미적용)

데이터/UI 분리는 적용하지 않았습니다. 지금은 `vietnam/index.html` 한 파일에
데이터와 UI가 함께 있습니다. 다만 데이터가 `{ko, vi, pron}` 구조로
정리되었으므로, `data/vietnam.js`로 잘라내는 작업은 이제 기계적입니다.

`fetch()` + `.json`은 `file://`에서 CORS로 막히므로, 폴더째 복사해 쓸
가능성이 있다면 `.json` 대신 `data/vietnam.js`에 `const VIETNAM_DATA = {...}`
형태로 두고 `<script>`로 불러오는 방식을 권합니다.
