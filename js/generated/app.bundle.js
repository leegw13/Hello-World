/* Generated by scripts/build-file-bundle.mjs. Do not edit directly. */
(() => {
// Source: js/config.js
const LANGUAGES = {
  id: {
    dataUrl: "./data/indonesia.json",
    title: "Halo Indonesia",
    subtitle: "인도네시아 현장 회화 도우미",
    label: "Bahasa Indonesia",
    flag: "🇮🇩",
    targetKey: "id",
    historyKey: "helloworld-id-history",
    translateCode: "id"
  },
  vi: {
    dataUrl: "./data/vietnam.json",
    title: "Xin chào Việt Nam",
    subtitle: "베트남 현장 회화 도우미",
    label: "Tiếng Việt",
    flag: "🇻🇳",
    targetKey: "vi",
    historyKey: "helloworld-vi-history",
    translateCode: "vi"
  }
};

function resolveLanguage() {
  const requested = new URLSearchParams(location.search).get("lang") || document.documentElement.dataset.lang;
  return Object.hasOwn(LANGUAGES, requested) ? requested : "id";
}


// Source: js/languages/kiosk.js
function topicParticle(word) {
  const last = word.charCodeAt(word.length - 1);
  return last >= 0xac00 && last <= 0xd7a3 && (last - 0xac00) % 28 !== 0 ? "은" : "는";
}
const locations = {
  id: [["화장실", "toilet", "똘렛"], ["편의점", "minimarket", "미니마르껫"], ["식당", "restoran", "레스또란"], ["카페", "kafe", "까페"], ["약국", "apotek", "아뽀떽"], ["병원", "rumah sakit", "루마 사낏"], ["ATM", "ATM", "아떼엠"], ["출구", "pintu keluar", "삔뚜 끌루아르"], ["공항", "bandara", "반다라"], ["버스 정류장", "halte bus", "할떼 부스"], ["기차역", "stasiun kereta", "스타시운 끄레따"], ["호텔", "hotel", "호텔"]],
  vi: [["화장실", "nhà vệ sinh", "냐 베 신"], ["편의점", "cửa hàng tiện lợi", "끄어 항 띠엔 러이"], ["식당", "nhà hàng", "냐 항"], ["카페", "quán cà phê", "꽌 까 페"], ["약국", "nhà thuốc", "냐 투옥"], ["병원", "bệnh viện", "베인 비엔"], ["ATM", "máy ATM", "마이 에이티엠"], ["출구", "lối ra", "로이 자"], ["공항", "sân bay", "선 바이"], ["버스 정류장", "trạm xe buýt", "짬 쎄 부잇"], ["기차역", "ga tàu", "가 따우"], ["호텔", "khách sạn", "칵 싼"]]
};

function restaurant(language) {
  const vietnamese = language === "vi";
  return {
    title: "메뉴와 주문 옵션을 선택하세요",
    items: vietnamese ? [["쌀국수", "phở", "퍼"], ["분짜", "bún chả", "분 짜"], ["반미", "bánh mì", "바인 미"], ["껌떰", "cơm tấm", "껌 떰"], ["월남쌈", "gỏi cuốn", "고이 꾸온"], ["물", "nước", "느억"]] : [["나시고렝", "Nasi goreng", "나시 고렝"], ["미고렝", "Mie goreng", "미 고렝"], ["사테", "Sate", "사떼"], ["박소", "Bakso", "박소"], ["밥", "Nasi", "나시"], ["물", "Air", "아이르"]],
    options: vietnamese ? [["기본 주문", "", ""], ["맵지 않게", "không cay", "콩 까이"], ["포장", "mang đi", "망 디"]] : [["기본 주문", "", ""], ["맵지 않게", "jangan pedas", "장안 뻐다스"], ["포장", "dibungkus", "디붕꾸스"]],
    sentence(item, option) {
      const ko = `${item[0]}${option[0] === "기본 주문" ? "" : `, ${option[0]}`} 주세요.`;
      return vietnamese ? { ko, vi: `Cho tôi ${item[1]}${option[1] ? ` ${option[1]}` : ""}.`, pron: `쪼 또이 ${item[2]}${option[2] ? ` ${option[2]}` : ""}` } : { ko, id: `Saya mau ${item[1]}${option[1] ? `, ${option[1]}` : ""}, ya.`, pron: `사야 마우 ${item[2]}${option[2] ? `, ${option[2]}` : ""} 야` };
    }
  };
}

function cafe(language) {
  const vietnamese = language === "vi";
  return {
    title: "메뉴에 맞는 주문 옵션을 선택하세요",
    items: vietnamese ? [["연유 커피", "cà phê sữa", "까 페 쓰어", "coffee"], ["블랙 커피", "cà phê đen", "까 페 덴", "coffee"], ["코코넛 커피", "cà phê dừa", "까 페 즈어", "coffee"], ["아메리카노", "americano", "아메리까노", "coffee"], ["에스프레소", "espresso", "에스프레소", "coffee"], ["밀크티", "trà sữa", "짜 쓰어", "tea"], ["아이스티", "trà đá", "짜 다", "cold"], ["차", "trà", "짜", "tea"], ["생과일주스", "nước ép trái cây", "느억 엡 짜이 꺼이", "juice"], ["스무디", "sinh tố", "신 또", "cold"], ["생수", "nước suối", "느억 쑤오이", "water"], ["케이크", "bánh ngọt", "바인 응옷", "food"], ["반미", "bánh mì", "바인 미", "food"]] : [["아메리카노", "Americano", "아메리까노", "coffee"], ["카페라떼", "Café latte", "까페 라떼", "coffee"], ["카푸치노", "Cappuccino", "까푸치노", "coffee"], ["에스프레소", "Espresso", "에스쁘레소", "coffee"], ["아이스커피", "Es kopi", "에스 꼬삐", "cold"], ["차", "Teh", "떼", "tea"], ["아이스티", "Es teh", "에스 떼", "cold"], ["핫초콜릿", "Cokelat panas", "초끌랏 빠나스", "hot"], ["주스", "Jus", "주스", "juice"], ["스무디", "Smoothie", "스무디", "cold"], ["생수", "Air mineral", "아이르 미네랄", "water"], ["케이크", "Kue", "꾸에", "food"], ["크루아상", "Croissant", "크루아상", "food"]],
    options: vietnamese ? [["기본", "", "", ["coffee", "cold", "tea", "juice", "water", "food"]], ["아이스", "đá", "다", ["coffee", "tea"]], ["뜨겁게", "nóng", "농", ["coffee", "tea"]], ["작은 사이즈", "cỡ nhỏ", "꺼 뇨", ["coffee", "cold", "tea", "juice"]], ["큰 사이즈", "cỡ lớn", "꺼 런", ["coffee", "cold", "tea", "juice"]], ["설탕 조금", "ít đường", "잇 드엉", ["coffee", "tea", "juice"]], ["설탕 없이", "không đường", "콩 드엉", ["coffee", "tea", "juice"]], ["얼음 없이", "không đá", "콩 다", ["cold", "juice"]]] : [["기본", "", "", ["coffee", "cold", "tea", "hot", "juice", "water", "food"]], ["아이스", "dingin", "딩인", ["coffee", "tea"]], ["뜨겁게", "panas", "빠나스", ["coffee", "tea"]], ["작은 사이즈", "ukuran kecil", "우꾸란 끄찔", ["coffee", "cold", "tea", "hot", "juice"]], ["큰 사이즈", "ukuran besar", "우꾸란 브사르", ["coffee", "cold", "tea", "hot", "juice"]], ["설탕 조금", "sedikit gula", "스디낏 굴라", ["coffee", "tea", "hot", "juice"]], ["설탕 없이", "tanpa gula", "딴빠 굴라", ["coffee", "tea", "hot", "juice"]], ["얼음 없이", "tanpa es", "딴빠 에스", ["cold", "juice"]]],
    sentence(item, option) {
      const ko = `${option[0] === "기본" ? "" : `${option[0]} `}${item[0]} 주세요.`;
      return vietnamese ? { ko, vi: `Cho tôi ${item[1]}${option[1] ? ` ${option[1]}` : ""}.`, pron: `쪼 또이 ${item[2]}${option[2] ? ` ${option[2]}` : ""}` } : { ko, id: `Saya mau ${item[1]}${option[1] ? ` ${option[1]}` : ""}, ya.`, pron: `사야 마우 ${item[2]}${option[2] ? ` ${option[2]}` : ""} 야` };
    }
  };
}

function locationKiosk(language) {
  const vietnamese = language === "vi";
  return {
    title: "장소를 선택하면 두 가지 질문이 표시됩니다",
    items: locations[language],
    options: [],
    sentences(item) {
      if (vietnamese) return [
        { ko: `${item[0]}${topicParticle(item[0])} 어디예요?`, vi: `${item[1]} ở đâu?`, pron: `${item[2]} 어 더우?` },
        { ko: `${item[0]}에 어떻게 가나요?`, vi: `Đi đến ${item[1]} như thế nào?`, pron: `디 덴 ${item[2]} 느 테 나오?` }
      ];
      return [
        { ko: `${item[0]}${topicParticle(item[0])} 어디인가요?`, id: `Di mana ${item[1]}?`, pron: `디 마나 ${item[2]}?` },
        { ko: `${item[0]}에 어떻게 가나요?`, id: `Bagaimana cara ke ${item[1]}?`, pron: `바가이마나 짜라 끄 ${item[2]}?` }
      ];
    }
  };
}

const KIOSKS = {
  id: { restaurant: restaurant("id"), cafe: cafe("id"), location: locationKiosk("id") },
  vi: { restaurant: restaurant("vi"), cafe: cafe("vi"), location: locationKiosk("vi") }
};


// Source: js/services/data-loader.js
async function loadLanguageData(config) {
  if (location.protocol === "file:") {
    const localData = window.HELLOWORLD_DATA;
    if (!localData) throw new Error("로컬 언어 데이터를 불러오지 못했습니다.");
    return localData;
  }
  const response = await fetch(config.dataUrl, { cache: "no-cache" });
  if (!response.ok) throw new Error(`언어 데이터 요청 실패 (${response.status})`);
  const data = await response.json();
  if (!Array.isArray(data.basic) || !data.topics || !Array.isArray(data.vocab)) {
    throw new Error("언어 데이터 형식이 올바르지 않습니다.");
  }
  return data;
}


// Source: js/services/history.js
function createHistory(storageKey) {
  let entries = [];
  try { entries = JSON.parse(localStorage.getItem(storageKey) || "[]"); } catch { entries = []; }
  return {
    all: () => entries.slice(),
    add(entry) {
      entries = [entry, ...entries.filter(item => item.input !== entry.input || item.direction !== entry.direction)].slice(0, 30);
      try { localStorage.setItem(storageKey, JSON.stringify(entries)); } catch { /* storage is optional */ }
    },
    clear() {
      entries = [];
      try { localStorage.removeItem(storageKey); } catch { /* storage is optional */ }
    }
  };
}


// Source: js/services/translator.js
function normalize(value) {
  return String(value || "").toLocaleLowerCase().replace(/[\s.,!?~·"'’“”()\-]/g, "");
}

function collectPhrases(data, targetKey) {
  const rows = [...(data.vocab || []), ...(data.extra || [])];
  for (const group of data.basic || []) {
    rows.push(...(group.phrases || []));
    for (const sub of group.subcategories || []) rows.push(...(sub.phrases || []));
  }
  for (const topic of Object.values(data.topics || {})) {
    for (const question of topic.q || []) {
      rows.push(question);
      for (const choice of question.choices || []) {
        rows.push(choice, ...(choice.followups || []));
      }
    }
  }
  return rows.filter(row => row.ko && row[targetKey]);
}

function createTranslator(data, config) {
  const phrases = collectPhrases(data, config.targetKey);

  function local(text, reverse) {
    const key = reverse ? config.targetKey : "ko";
    const query = normalize(text);
    const exact = phrases.find(row => normalize(row[key]) === query);
    if (exact) return { output: exact[reverse ? "ko" : config.targetKey], pronunciation: reverse ? "" : exact.pron, provider: "offline" };
    return null;
  }

  async function online(text, reverse) {
    const source = reverse ? config.translateCode : "ko";
    const target = reverse ? "ko" : config.translateCode;
    const url = `https://api.mymemory.translated.net/get?q=${encodeURIComponent(text)}&langpair=${source}|${target}`;
    const response = await fetch(url);
    if (!response.ok) throw new Error("온라인 번역 서비스에 연결할 수 없습니다.");
    const payload = await response.json();
    const output = payload?.responseData?.translatedText;
    if (!output) throw new Error("번역 결과가 없습니다.");
    return { output, pronunciation: "", provider: "online" };
  }

  return {
    async translate(text, reverse) {
      return local(text, reverse) || online(text, reverse);
    }
  };
}


// Source: js/app.js

const language = resolveLanguage();
const config = LANGUAGES[language];
const languageUx = KIOSKS[language];
const state = { data: null, reverse: false, activeTopic: null, translator: null, history: createHistory(config.historyKey), lastResult: null };
const $ = selector => document.querySelector(selector);

function escapeHtml(value) {
  return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}

function localText(row) {
  return row?.[config.targetKey] || "";
}

function phraseHtml(row, className = "phrase") {
  return `<div class="${className}"><div class="ko">${escapeHtml(row.ko)}</div><div class="${config.targetKey}">${escapeHtml(localText(row))}</div>${row.pron ? `<div class="pron">${escapeHtml(row.pron)}</div>` : ""}</div>`;
}

function renderBasic() {
  const icons = ["👋", "🙏", "🍽️", "☕", "📍", "💬", "🔤"];
  $("#basicList").innerHTML = state.data.basic.map((group, index) => {
    let content = group.subcategories?.length
      ? `<div class="basic-sub-list">${group.subcategories.map((sub, subIndex) => `<div class="basic-subitem"><button class="basic-sub-toggle" data-sub="${index}-${subIndex}" type="button"><span class="basic-sub-title">🔤 ${escapeHtml(sub.title)}</span><span class="basic-count">${sub.phrases.length}</span><span class="basic-sub-arrow">⌄</span></button><div class="basic-sub-panel" data-sub-panel="${index}-${subIndex}"><div class="phrase-list">${sub.phrases.map(row => phraseHtml(row)).join("")}</div></div></div>`).join("")}</div>`
      : `<div class="phrase-list">${(group.phrases || []).map(row => phraseHtml(row)).join("")}</div>`;
    if (index === 2) content = renderKiosk("restaurant", content);
    if (index === 3) content = renderKiosk("cafe", content);
    if (index === 4) content = renderKiosk("location", content);
    const count = group.phrases?.length || group.subcategories?.reduce((sum, sub) => sum + sub.phrases.length, 0) || 0;
    return `<div class="basic-item"><button class="basic-toggle" data-basic="${index}" type="button"><span class="basic-icon">${icons[index] || "💡"}</span><span class="basic-title"><span class="basic-ko">${escapeHtml(group.korean)}</span><span class="basic-id">${escapeHtml(group.local)}</span></span><span class="basic-count">${count}</span><span class="basic-arrow">⌄</span></button><div class="basic-panel" data-basic-panel="${index}">${content}</div></div>`;
  }).join("");
}

function renderKiosk(type, fallbackPhrases) {
  const kiosk = languageUx[type];
  const locationMode = type === "location";
  const icon = type === "restaurant" ? "🍽️" : type === "cafe" ? "☕" : "📍";
  const optionStep = locationMode ? "" : `<div class="kiosk-step"><div class="kiosk-step-label">2. 주문 옵션</div><div class="kiosk-menu">${kiosk.options.map((option, index) => `<button class="kiosk-choice" data-kiosk-option="${type}-${index}" type="button">${escapeHtml(option[0])}</button>`).join("")}</div></div>`;
  return `<div class="basic-kiosk-card" data-kiosk="${type}"><div class="basic-kiosk-title">${icon} ${escapeHtml(kiosk.title)}</div><div class="kiosk-step"><div class="kiosk-step-label">${locationMode ? "장소를 선택하세요" : "1. 메뉴"}</div><div class="kiosk-menu">${kiosk.items.map((item, index) => `<button class="kiosk-choice" data-kiosk-item="${type}-${index}" type="button">${escapeHtml(item[0])}</button>`).join("")}</div></div>${optionStep}<div class="kiosk-sentence-area" data-kiosk-result="${type}"><div class="kiosk-placeholder">${locationMode ? "장소를" : "메뉴와 옵션을"} 선택하면 사용할 표현이 표시됩니다.</div></div></div><div class="basic-extra-title">기본 표현</div>${fallbackPhrases}`;
}

function resetKiosks() {
  document.querySelectorAll("[data-kiosk]").forEach(root => {
    delete root.dataset.item;
    delete root.dataset.option;
    root.querySelectorAll(".kiosk-choice.selected").forEach(button => button.classList.remove("selected"));
    root.querySelectorAll("[data-kiosk-option]").forEach(button => { button.hidden = false; });
    const result = root.querySelector("[data-kiosk-result]");
    const locationMode = root.dataset.kiosk === "location";
    result.innerHTML = `<div class="kiosk-placeholder">${locationMode ? "장소를" : "메뉴와 옵션을"} 선택하면 사용할 표현이 표시됩니다.</div>`;
  });
}

function filterKioskOptions(type, itemIndex) {
  const root = document.querySelector(`[data-kiosk="${type}"]`);
  const category = languageUx[type].items[itemIndex][3];
  if (!category) return;
  root.querySelectorAll("[data-kiosk-option]").forEach((button, optionIndex) => {
    const allowed = languageUx[type].options[optionIndex][3];
    button.hidden = Array.isArray(allowed) && !allowed.includes(category);
  });
  const selected = root.querySelector("[data-kiosk-option].selected");
  if (selected?.hidden) { selected.classList.remove("selected"); delete root.dataset.option; }
}

function updateKiosk(type) {
  const root = document.querySelector(`[data-kiosk="${type}"]`);
  const itemIndex = Number(root.dataset.item);
  const optionIndex = Number(root.dataset.option);
  if (!Number.isInteger(itemIndex)) return;
  const kiosk = languageUx[type];
  if (type === "location") {
    const sentences = kiosk.sentences(kiosk.items[itemIndex]);
    root.querySelector(`[data-kiosk-result="${type}"]`).innerHTML = sentences.map((sentence, index) => `<div class="kiosk-sentence"><div class="kiosk-sentence-label">${index === 0 ? "어디예요?" : "어떻게 가나요?"}</div>${phraseHtml(sentence, "kiosk-phrase")}</div>`).join("");
    return;
  }
  if (!Number.isInteger(optionIndex)) return;
  const sentence = kiosk.sentence(kiosk.items[itemIndex], kiosk.options[optionIndex]);
  root.querySelector(`[data-kiosk-result="${type}"]`).innerHTML = `<div class="kiosk-sentence"><div class="kiosk-sentence-label">이 표현을 보여주거나 읽어주세요</div>${phraseHtml(sentence, "kiosk-phrase")}</div>`;
}

function renderTopics() {
  $("#topicPicker").innerHTML = Object.entries(state.data.topics).map(([key, topic]) => {
    const title = topic.title || key;
    const emoji = title.match(/^\p{Extended_Pictographic}/u)?.[0] || "💬";
    return `<button class="topic" data-topic="${escapeHtml(key)}" type="button"><span class="emoji">${emoji}</span><div class="ko">${escapeHtml(title)}</div><div class="${config.targetKey}">${topic.q?.length || 0} questions</div></button>`;
  }).join("");
}

function renderQuestionList(key) {
  const topic = state.data.topics[key];
  state.activeTopic = key;
  $("#topicPicker").hidden = true;
  $("#talk").classList.add("talk-detail");
  $("#talkFlow").innerHTML = `<button class="back" data-action="topics" type="button"><span class="back-arrow">‹</span> 주제 선택</button><div class="section-title"><h2>${escapeHtml(topic.title)}</h2><p>질문을 누르면 답변 예시가 열립니다</p></div><div class="question-btn-list">${topic.q.map((question, index) => `<div class="q-item"><button class="question-btn" data-question="${index}" type="button"><span class="qnum">${index + 1}</span><span class="qtext"><span class="t-${config.targetKey}">${escapeHtml(localText(question))}</span><span class="t-pron">${escapeHtml(question.pron || "")}</span><span class="t-ko">${escapeHtml(question.ko)}</span></span><span class="qtoggle">⌄</span></button><div class="q-panel" data-question-panel="${index}">${renderChoices(question, index)}</div></div>`).join("")}</div>`;
}

function renderChoices(question, questionIndex) {
  return `<div class="choice-label">ANSWER EXAMPLES</div><div class="choice-list">${(question.choices || []).map((choice, choiceIndex) => `<div class="choice-item"><button class="choice-btn" data-choice="${questionIndex}-${choiceIndex}" type="button"><span class="ctext"><span class="t-${config.targetKey}">${escapeHtml(localText(choice))}</span><span class="t-pron">${escapeHtml(choice.pron || "")}</span><span class="t-ko">${escapeHtml(choice.ko)}</span></span><span class="ctoggle">⌄</span></button><div class="followup-list" data-followups="${questionIndex}-${choiceIndex}"><div class="fl-label">FOLLOW-UP QUESTIONS</div>${(choice.followups || []).map((item, index) => `<div class="followup-item"><div class="fnum">${index + 1}</div><span class="t-${config.targetKey}">${escapeHtml(localText(item))}</span><span class="t-pron">${escapeHtml(item.pron || "")}</span><span class="t-ko">${escapeHtml(item.ko)}</span></div>`).join("")}</div></div>`).join("")}</div>`;
}

function showTab(tab) {
  closeAllAccordions();
  resetKiosks();
  if (tab !== "talk") resetTopics();
  for (const section of document.querySelectorAll("main > .screen")) section.hidden = section.id !== tab;
  for (const button of document.querySelectorAll("[data-tab]")) button.classList.toggle("active", button.dataset.tab === tab);
  scrollTo({ top: 0, behavior: "smooth" });
}

function closeAllAccordions(exceptButton = null, exceptPanel = null) {
  for (const button of document.querySelectorAll(".basic-toggle.open,.basic-sub-toggle.open,.question-btn.open,.choice-btn.open")) {
    if (button !== exceptButton) button.classList.remove("open");
  }
  for (const panel of document.querySelectorAll(".basic-panel.open,.basic-sub-panel.open,.q-panel.open,.followup-list.open")) {
    if (panel !== exceptPanel) panel.classList.remove("open");
  }
}

function resetTopics() {
  state.activeTopic = null;
  $("#topicPicker").hidden = false;
  $("#talkFlow").innerHTML = "";
  $("#talk").classList.remove("talk-detail");
}

function togglePair(button, panel) {
  const open = !button.classList.contains("open");
  if (button.classList.contains("basic-toggle")) {
    closeAllAccordions(button, panel);
  } else if (button.classList.contains("basic-sub-toggle")) {
    const parent = button.closest(".basic-sub-list");
    parent.querySelectorAll(".basic-sub-toggle.open").forEach(item => { if (item !== button) item.classList.remove("open"); });
    parent.querySelectorAll(".basic-sub-panel.open").forEach(item => { if (item !== panel) item.classList.remove("open"); });
  } else if (button.classList.contains("question-btn")) {
    const parent = button.closest(".question-btn-list");
    parent.querySelectorAll(".question-btn.open,.choice-btn.open").forEach(item => { if (item !== button) item.classList.remove("open"); });
    parent.querySelectorAll(".q-panel.open,.followup-list.open").forEach(item => { if (item !== panel) item.classList.remove("open"); });
  } else if (button.classList.contains("choice-btn")) {
    const parent = button.closest(".choice-list");
    parent.querySelectorAll(".choice-btn.open").forEach(item => { if (item !== button) item.classList.remove("open"); });
    parent.querySelectorAll(".followup-list.open").forEach(item => { if (item !== panel) item.classList.remove("open"); });
  }
  button.classList.toggle("open", open);
  panel?.classList.toggle("open", open);
}

function updateDirection() {
  $("#dirFrom").textContent = state.reverse ? `${config.flag} ${config.label}` : "🇰🇷 한국어";
  $("#dirTo").textContent = state.reverse ? "🇰🇷 한국어" : `${config.flag} ${config.label}`;
}

function renderResult(input, result) {
  state.lastResult = { input, ...result };
  const tag = result.provider === "offline" ? "오프라인 문구" : "온라인 번역";
  const tagClass = result.provider === "offline" ? "tag-offline" : "tag-online";
  $("#trResult").innerHTML = `<div class="result-card"><span class="result-tag ${tagClass}">${tag}</span><div class="result-src">${escapeHtml(input)}</div><button class="result-main" data-show-big type="button" aria-label="번역 결과 크게 보기">${escapeHtml(result.output)}</button>${result.pronunciation ? `<div class="result-pron">${escapeHtml(result.pronunciation)}</div>` : ""}<div class="result-actions"><button class="res-btn primary" data-copy="${escapeHtml(result.output)}" type="button">복사</button><button class="res-btn" data-fill="${escapeHtml(result.output)}" type="button">다시 사용</button></div><div class="result-big-hint">번역 문장을 누르면 크게 볼 수 있습니다.</div></div>`;
}

function openBigView() {
  if (!state.lastResult) return;
  $("#bigText").textContent = state.lastResult.output;
  $("#bigSub").textContent = state.lastResult.pronunciation || state.lastResult.input;
  $("#bigView").hidden = false;
  document.body.classList.add("big-view-open");
  $("[data-close-big]").focus();
}

function closeBigView() {
  $("#bigView").hidden = true;
  document.body.classList.remove("big-view-open");
  $("[data-show-big]")?.focus();
}

function renderHistory() {
  const entries = state.history.all();
  $("#trHistoryWrap").hidden = entries.length === 0;
  $("#trHistory").innerHTML = entries.map((entry, index) => `<button class="hist-item" data-history="${index}" type="button"><div class="h-out">${escapeHtml(entry.output)}</div><div class="h-in">${escapeHtml(entry.input)}</div></button>`).join("");
}

async function runTranslate() {
  const input = $("#srcText").value.trim();
  if (!input) return;
  const button = $("#translateBtn");
  button.disabled = true;
  button.textContent = "번역 중…";
  try {
    const result = await state.translator.translate(input, state.reverse);
    renderResult(input, result);
    state.history.add({ input, output: result.output, pronunciation: result.pronunciation, direction: state.reverse ? "reverse" : "forward" });
    renderHistory();
  } catch (error) {
    $("#trResult").innerHTML = `<div class="result-card"><span class="result-tag tag-error">번역 실패</span><div class="result-main">${escapeHtml(error.message)}</div></div>`;
  } finally {
    button.disabled = false;
    button.textContent = "번역하기";
  }
}

function bindEvents() {
  document.addEventListener("click", async event => {
    const target = event.target.closest("button");
    if (!target) return;
    if (target.dataset.tab) return showTab(target.dataset.tab);
    if (target.dataset.basic) { resetKiosks(); return togglePair(target, document.querySelector(`[data-basic-panel="${target.dataset.basic}"]`)); }
    if (target.dataset.sub) return togglePair(target, document.querySelector(`[data-sub-panel="${target.dataset.sub}"]`));
    if (target.dataset.topic) return renderQuestionList(target.dataset.topic);
    if (target.dataset.action === "topics") { resetTopics(); return; }
    if (target.dataset.question) return togglePair(target, document.querySelector(`[data-question-panel="${target.dataset.question}"]`));
    if (target.dataset.choice) return togglePair(target, document.querySelector(`[data-followups="${target.dataset.choice}"]`));
    if (target.hasAttribute("data-show-big")) { openBigView(); return; }
    if (target.hasAttribute("data-close-big")) { closeBigView(); return; }
    if (target.dataset.kioskItem) {
      const [type, index] = target.dataset.kioskItem.split("-");
      const root = document.querySelector(`[data-kiosk="${type}"]`);
      root.dataset.item = index;
      root.querySelectorAll("[data-kiosk-item]").forEach(button => button.classList.toggle("selected", button === target));
      filterKioskOptions(type, Number(index));
      updateKiosk(type);
      return;
    }
    if (target.dataset.kioskOption) {
      const [type, index] = target.dataset.kioskOption.split("-");
      const root = document.querySelector(`[data-kiosk="${type}"]`);
      root.dataset.option = index;
      root.querySelectorAll("[data-kiosk-option]").forEach(button => button.classList.toggle("selected", button === target));
      updateKiosk(type);
      return;
    }
    if (target.id === "swapBtn") { state.reverse = !state.reverse; updateDirection(); return; }
    if (target.id === "translateBtn") return runTranslate();
    if (target.id === "clearBtn") { $("#srcText").value = ""; $("#trResult").innerHTML = ""; $("#charCount").textContent = "0자"; return; }
    if (target.id === "historyClear") { state.history.clear(); renderHistory(); return; }
    if (target.dataset.copy) { await navigator.clipboard.writeText(target.dataset.copy); target.textContent = "복사됨"; return; }
    if (target.dataset.fill) { $("#srcText").value = target.dataset.fill; $("#charCount").textContent = `${target.dataset.fill.length}자`; return; }
    if (target.dataset.history) { const item = state.history.all()[Number(target.dataset.history)]; $("#srcText").value = item.input; $("#charCount").textContent = `${item.input.length}자`; }
  });
  $("#srcText").addEventListener("input", event => { $("#charCount").textContent = `${event.target.value.length}자`; });
  $("#srcText").addEventListener("keydown", event => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); runTranslate(); } });
  $("#bigView").addEventListener("click", event => { if (event.target === $("#bigView")) closeBigView(); });
  document.addEventListener("keydown", event => { if (event.key === "Escape" && !$("#bigView").hidden) closeBigView(); });
}

async function start() {
  try {
    state.data = await loadLanguageData(config);
    state.translator = createTranslator(state.data, config);
    document.title = `${config.title} · HelloWorld`;
    $("#appTitle").textContent = config.title;
    $("#appSubtitle").textContent = config.subtitle;
    $("#guideLanguage").textContent = config.label;
    renderBasic();
    renderTopics();
    updateDirection();
    renderHistory();
    bindEvents();
    $("#app").setAttribute("aria-busy", "false");
    if ("serviceWorker" in navigator) navigator.serviceWorker.register("./sw.js");
  } catch (error) {
    $("#appSubtitle").textContent = error.message;
    $("#app").setAttribute("aria-busy", "false");
  }
}

start();

})();
